DONE      Change selected canvas by click
DONE  add thicker border to selected canvasClass (add class activePanel)
DONE  if only one canvas left, set that one as the active one on deletion of the second canvas.
      add listeners to list items/buttons
      read through Fabric documentation
      drag, drop comic background images
        

DONE      set canvas elements to flow (multiple canvases in row),
      set canvas max width,
DONE  get toolbar list to float:right over canvas, within wrapper div
-----------------------------------------------------
DONE  add icons to toolbar
      add event handlers to toolbar icons
      on icon click, create object in selected canvas (moveable)
      add ability to snap text to bubble
DONE  map 'delete' functionality to delete button
      add tooltips, alt text to buttons
